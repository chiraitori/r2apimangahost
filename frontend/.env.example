# URL trỏ tới Backend Go đã deploy (Render, Railway, VPS, Fly.io, v.v.)
# Nếu chạy local: http://localhost:8080/api/v1
# Nếu deploy Vercel: https://api.yourdomain.com/api/v1 (hoặc URL backend của bạn)
VITE_API_URL=http://localhost:8080/api/v1
